# AGENTS.md

## 项目定位

这是一个基于 Tauri 的跨平台桌面万能工具箱。

项目会长期增加彼此可能完全无关的功能，例如：

* Git 管理
* 图片处理
* 库存管理
* 开发者工具
* 文件工具
* 数据转换
* 小游戏
* 其他个人效率工具

这些功能共同运行在同一个桌面应用中，但不应因此产生不必要的业务耦合。

项目长期目标是：

> 在保持统一桌面体验的同时，让新的工具能够低成本加入，并尽量不影响已有工具。

当前采用 **模块化单体（Modular Monolith）**。

除非任务明确要求，不要引入运行时插件系统、微前端、独立子进程架构或其他复杂扩展机制。

---

## 核心原则

### 1. 功能按模块组织

主要功能应拥有自己的模块边界。

前端原则上放在：

```text
src/modules/<module>/
```

需要 Rust 原生能力时，相关实现原则上放在：

```text
src-tauri/src/modules/<module>/
```

例如：

```text
src/modules/git/
src/modules/image/
src/modules/inventory/
src/modules/snake/
```

一个模块应尽量拥有自己的：

* 页面
* 组件
* 状态
* 类型
* 服务
* 模块内部工具函数

不要把某个模块专属的代码提前放进全局 `shared`。

只有确实被多个无关模块复用的能力才进入共享层。

---

### 2. 保持模块独立

不要让无关模块直接依赖彼此的内部实现。

例如通常不应出现：

```text
git -> inventory
image -> git
snake -> inventory
```

如果多个模块确实需要同一种通用能力，再提取公共实现。

不要为了消除少量重复代码提前制造抽象。

---

### 3. Shell 与业务模块分离

应用级能力属于 App Shell，例如：

* 主布局
* 导航
* 路由
* 全局设置
* 主题
* 工具搜索
* 收藏
* 最近使用
* Command Palette
* 模块注册

具体业务属于各自 Module。

Shell 不应知道某个工具内部如何工作。

---

## Module Registry

项目可以维护一个简单的模块注册表，用于导航、路由和工具发现。

例如：

```ts
interface ToolboxModule {
  id: string;
  name: string;
  route: string;
  category?: string;
  icon?: ReactNode;
}
```

保持它简单。

Module Registry 是应用内部的模块清单，不是插件系统。

不要自行增加复杂的：

* ModuleManager
* ModuleLoader
* ModuleLifecycle
* Dependency Injection
* Event Bus
* 动态插件发现

除非真实需求已经出现。

---

## 前端与 Rust 的边界

根据问题本身选择实现位置，不要因为这是 Tauri 项目就默认把逻辑写进 Rust。

适合前端的内容包括：

* UI
* 交互
* 页面状态
* 浏览器能够良好完成的轻量逻辑
* 小游戏等纯前端功能

适合 Rust / Tauri 的内容包括：

* 文件系统
* Git 操作
* 系统能力
* 原生进程
* 性能敏感处理
* 大量图片处理
* 需要原生库的能力

例如：

```text
Snake
→ 前端即可

Git
→ 前端 UI + Tauri/Rust Git 能力

图片批处理
→ 前端 UI + Rust 图片处理

库存
→ 前端 UI + 持久化
```

根据实际需求判断，不要求所有模块使用相同结构。

---

## 持久化

持久化方式根据数据性质选择。

简单偏好和 UI 状态不需要为了统一而全部进入数据库。

对于结构化、长期存在的业务数据，默认优先考虑 SQLite，除非当前需求表明其他方案更合适。

模块专属数据应尽量由模块自己管理。

---

## 工作区状态

部分工具可能具有独立 Workspace，例如：

* Git Repository
* Inventory Project
* Image Project
* Database Connection

如果某个模块天然存在 Workspace，可以保存 Workspace 独立状态。

例如 Git 仓库可以分别记住：

* 当前页面
* Branch 过滤条件
* Author 过滤条件
* 搜索内容
* 选中的 Commit
* 滚动位置

但不要强迫所有工具都套用 Workspace 模型。

---

## 架构决策原则

优先：

1. 正确
2. 简单
3. 清晰
4. 可维护
5. 模块边界明确

在多个方案都可行时，选择当前需求下最简单的方案。

不要为尚未出现的问题提前建设基础设施。

尤其避免因为“以后可能需要”而主动引入：

* 插件框架
* DI 框架
* Event Bus
* 通用 CRUD 框架
* 复杂 Repository 层
* 多层 Provider / Manager / Service 包装
* 自研框架式基础设施

未来需要时再演进。

---

## 修改现有代码

### UI 设计约定

新增或修改 UI 前，阅读 `docs/ui-design.md`，按其中的 Codex 风格桌面工作区规范实现。

* 视觉变量统一来自 `src/styles/tokens.css`，原生控件与公共布局复用 `src/styles/ui.css` 的 `ui-` 类。
* 模块 CSS 只处理模块专属布局，不另建配色、字号、按钮或主题，不覆盖全局 token。
* 保持中性色、紧凑布局、细边框、小圆角；红色仅用于危险与错误，保留焦点和清晰状态文字。
* 更新公共设计规则时同步修改规范文档和公共样式；视觉修改需检查实际渲染及相关状态。
* 不为统一外观引入未经需要的 UI 框架或复杂组件封装。

处理任务时，根据任务范围自行阅读相关代码。

不要求每次修改前遍历整个仓库。

需要了解架构边界时，再查看对应架构文档。

修改应尽量集中在与当前任务相关的区域。

不要顺手进行与任务无关的大规模：

* 重构
* 重命名
* 格式化
* 目录迁移
* 依赖替换

如果现有结构合理，优先沿用。

如果现有结构阻碍当前任务，可以在当前任务需要的范围内改善。

---

## 不要机械服从示例结构

本文中的路径和代码只表示架构方向，不是必须机械匹配的模板。

实际仓库状态是真实依据。

不要仅仅为了符合本文示例而迁移已经工作的代码。

---

## 依赖

增加依赖前先判断：

* 现有依赖是否已经能够完成
* 标准库或平台能力是否已经能够完成
* 新依赖是否明显降低实现复杂度

不要为了很小的问题引入大型框架。

不要无理由替换当前已经工作的技术方案。

---

## 错误处理

用户可恢复的错误应给出清晰、可理解的信息。

内部错误应保留足够的调试上下文。

不要静默吞掉重要错误。

Rust 中，对正常可能发生的失败优先返回错误，不要依赖 `unwrap()`。

同时不要为了理论上的每一种异常路径加入大量当前没有价值的防御代码。

---

## 测试与验证

根据本次改动运行相关验证。

可以直接执行安全的本地构建、类型检查、lint、测试和 `cargo check`，无需为了每一步请求确认。

修复由本次修改导致的问题，并重新运行受影响的检查。

不要求为了一个局部修改机械运行所有无关测试。

如果项目环境导致某项验证无法执行，说明原因即可。

---

## 完成标准

收到实现任务后，不要只完成第一版代码就默认结束。

在任务范围明确且没有需要用户决策的问题时，应继续完成：

```text
实现
→ 运行
→ 检查结果
→ 修复本次修改导致的问题
→ 再验证
```

直到达到可正常交付的状态。

如果过程中发现需要改变产品行为、数据模型或重要架构方向，再停下来说明决策点。

对于普通实现细节，可以自行做出合理判断继续完成。

---

## 决策边界

可以自行决定：

* 局部代码组织
* 合理的组件拆分
* 内部变量和函数命名
* 当前需求范围内的小型重构
* 修复由本次修改直接造成的问题
* 运行安全的本地检查和测试

遇到以下情况时应说明，而不是擅自扩大任务：

* 明显改变产品行为
* 删除用户数据
* 修改持久化数据的兼容方式
* 引入新的大型框架
* 改变主要技术栈
* 大范围重构
* 明显超出当前需求的架构建设

---

## 开发方向

这个项目会持续增加新的工具。

衡量架构是否健康的一个重要标准是：

> 新增一个普通工具时，主要修改应该发生在这个工具自己的模块中，而不是迫使大量已有模块一起改变。

因此：

**保持模块独立，但不要为了模块化而过度抽象。**

**保持未来可扩展，但不要提前实现未来。**
